testing:
  build:
    dockerfile: ./ci/Dockerfile.testing
  encrypted_env_file: secrets.encrypted
package:
  build:
    dockerfile: ./ci/Dockerfile.package
github-pages:
  build:
    dockerfile: ./ci/Dockerfile.github-pages
